# Codex Skill Mapping

No command-specific Codex skill is required for `/ai-change`; use the repository command contract and `.claude/scripts/ai_change_governance.py`.

No command-specific Codex skill is required for `/ai-plan`; use the repository command contract, `docs/ai_plan_governance.md`, and `.claude/scripts/ai_plan_governance.py`. Template-derived projects should keep orchestration in the local MCP backend pattern rather than a Codex skill.
